local lspconfig = require("lspconfig")

-- Servers to be installed and set up
local servers = {
    "clangd",
    "lua_ls",
}

local function on_attach(_, _) end

for _, server in pairs(servers) do

end
